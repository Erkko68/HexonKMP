ktor {
    deployment {
        host = "0.0.0.0"
        host = ${?SERVER_BIND_HOST}
        port = 8081
        port = ${?SERVER_BIND_PORT}
    }
    application {
        modules = [ eric.bitria.hexonkmp.ApplicationKt.module ]
    }
}
